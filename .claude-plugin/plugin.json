{
  "name": "davinci-resolve",
  "version": "0.1.3",
  "description": "Control DaVinci Resolve from Claude — execute API calls, create timelines, manage media, render projects.\n\n## Prerequisites\n\n- [uv](https://docs.astral.sh/uv/) — Python package runner (auto-installs Python and dependencies)\n- DaVinci Resolve installed and running\n- Claude Desktop (with Cowork mode)\n\nIf you don't have uv:\n- macOS/Linux: curl -LsSf https://astral.sh/uv/install.sh | sh\n- Windows: powershell -ExecutionPolicy ByPass -c \"irm https://astral.sh/uv/install.ps1 | iex\"\n\n## Installation\n\n### Install via Marketplace (Recommended)\n\n1. Open **Claude Desktop** and switch to **Cowork** mode\n2. Click **Add Plugin** > **Personal** > **+** (plus)\n3. Select **Install from URL**\n4. Paste: `https://github.com/fenixstarlord/cowork-chameleonmarketplace`\n5. Select the **DaVinci Resolve** plugin and install it\n6. Run the setup script (see Register the MCP server below)\n\n### Install manually\n\n1. **Build the plugin zip**\n   - macOS/Linux: git clone https://github.com/fenixstarlord/cowork-davinciresolve.git && cd cowork-davinciresolve && ./package.sh\n   - Windows: git clone https://github.com/fenixstarlord/cowork-davinciresolve.git && cd cowork-davinciresolve && .\\package.ps1\n   - This creates davinci-resolve.zip. No other setup needed — uv auto-installs the mcp dependency on first run.\n\n2. **Upload to Claude Desktop**\n   - Open Claude Desktop and switch to Cowork mode\n   - Click Add Plugin > Personal > + (plus)\n   - Select Upload plugin\n   - Upload the davinci-resolve.zip file\n\n3. Run the setup script (see Register the MCP server below)\n\n### Register the MCP server\n\nCowork plugins run inside a sandboxed VM that cannot reach the local Resolve scripting API. You must register the MCP server in Claude Desktop's config so it runs natively on your machine.\n\n- macOS/Linux: cd <plugin-directory> && ./setup.sh\n- Windows: cd <plugin-directory> && .\\setup.ps1\n\nThe script installs uv if needed and adds the server to claude_desktop_config.json. Restart Claude Desktop after running it.\n\nTo uninstall: ./setup.sh --uninstall (macOS/Linux) or .\\setup.ps1 -Uninstall (Windows)\n\n### Start using it\n   - Make sure DaVinci Resolve is running\n   - Start a new Cowork session with the plugin enabled\n   - Ask Claude to do things in Resolve\n\n## Custom Resolve Script Path\n\nThe plugin auto-detects the Resolve scripting modules path. If your installation is non-standard, set the RESOLVE_SCRIPT_PATH environment variable in .mcp.json.\n\nDefault paths:\n- macOS: /Library/Application Support/Blackmagic Design/DaVinci Resolve/Developer/Scripting/Modules\n- Windows: C:\\ProgramData\\Blackmagic Design\\DaVinci Resolve\\Support\\Developer\\Scripting\\Modules\n- Linux: /opt/resolve/Developer/Scripting/Modules\n\n## Troubleshooting\n\n- \"Not connected to DaVinci Resolve\" — Make sure Resolve is running, then ask Claude to call refresh_connection\n- \"Could not import DaVinciResolveScript\" — Set RESOLVE_SCRIPT_PATH in .mcp.json\n- MCP server not starting — Confirm uv is installed (uv --version). Test manually with: uv run mcp_server.py\n\n## Disclaimer\n\nThis software was vibe coded with reckless optimism and minimal understanding of what's actually happening under the hood. The developer (generous term) cannot guarantee that anything works, will continue to work, or ever worked in the first place. Use at your own risk, amusement, or horror. By the way, this disclaimer was AI-generated, because that's how not involved I am in this code.",
  "author": {
    "name": "fenixstarlord"
  },
  "repository": "https://github.com/fenixstarlord/cowork-davinciresolve"
}
